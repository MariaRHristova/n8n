# Keep the repository focused on the exported workflows only.
# Track the workflow JSON files under /workflows and ignore the supporting docs.


# Optional supporting files are intentionally excluded from GitHub.
/docs/
/workflows/manifest.csv
